import { Component } from '@angular/core';

@Component({
  selector: 'cvm-login-callback',
  standalone: true,
  template: `<p>Login-Callback (Keycloak) – wird in Iteration 07 verdrahtet.</p>`
})
export class LoginCallbackComponent {}
